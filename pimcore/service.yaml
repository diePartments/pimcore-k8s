apiVersion: v1
kind: Service
metadata:
  labels:
    tier: pimcore
  name: pimcore
spec:
  selector:
    app: pimcore
    tier: pimcore
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80