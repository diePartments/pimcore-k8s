apiVersion: v1
kind: Service
metadata:
  labels:
    tier: backend
  name: pimcore
spec:
  selector:
    app: pimcore
    tier: backend
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80